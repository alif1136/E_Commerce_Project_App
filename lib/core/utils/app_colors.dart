import 'package:flutter/material.dart';

class AppColors {

  // Primary brand color (used in AppBar, buttons, highlights)
  static const Color primaryColor = Color(0xFF07AEAF);

  // Secondary accent color
  static const Color secondaryColor = Color(0xFF00C2C2);

  // Background color of the app
  static const Color backgroundColor = Color(0xFFF5F5F5);

  // Text colors
  static const Color textPrimary = Color(0xFF1F1F1F);
  static const Color textSecondary = Color(0xFF6E6E6E);

  // Border color
  static const Color borderColor = Color(0xFFE0E0E0);

  // Card background
  static const Color cardColor = Colors.white;

  // Icon color
  static const Color iconColor = Color(0xFF07AEAF);

  // Rating star color
  static const Color ratingColor = Colors.amber;

  // Error color
  static const Color errorColor = Colors.red;

  // Success color
  static const Color successColor = Colors.green;

  // Divider color
  static const Color dividerColor = Color(0xFFDDDDDD);
}